# Запрещаем прямой доступ к данным (admin.json, bans.json и т.п.)
# на случай, если docroot указывает не на public/.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
